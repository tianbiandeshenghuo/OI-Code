#include<bits/stdc++.h>

using namespace std;
const int MAXN = 2e5 + 5;
int t, n, m, a[MAXN];
void Solve(){
  
}
int main(){
  ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
  while (1){
	 system("./spj t.in t.out t.out");
  }
  return 0;
}
